#include<stdio.h>
int main()
{
	int a,b,c,d,e,i,f,g,N;
	scanf("%d",&N);
	if(N==3)
	{
		for(i=100;i<=999;i++)
		{a=i/100;
		 b=i%100/10;
		 c=i%10;
		 if(i==a*a*a+b*b*b+c*c*c)
		 printf("%3d\n",i);
		}
	}
    if(N==4)
    {
    	for(i=1000;i<=9999;i++)
    	{
	    	a=i/1000;
	    	b=i%1000/100;
	    	c=i%100/10;
	    	d=i%10;
	    	if(i==a*a*a*a+b*b*b*b+c*c*c*c+d*d*d*d)
	    	printf("%3d\n",i);
	    }
    }
    if(N==5)
    {
    	for(i=10000;i<=99999;i++)
    	{
	    	a=i/10000;
	    	b=i%10000/1000;
	    	c=i%1000/100;
	    	d=i%100/10;
	    	e=i%10;
	    	if(i==a*a*a*a*a+b*b*b*b*b+c*c*c*c*c+d*d*d*d*d+e*e*e*e*e)
	    	printf("%3d\n",i);
	    }
    }
    if(N==6)
    {
    	for(i=100000;i<=999999;i++)
    	{
	    	a=i/100000;
	    	b=i%100000/10000;
	    	c=i%10000/1000;
	    	d=i%1000/100;
	    	e=i%100/10;
	    	f=i%10;
	    	if(i==a*a*a*a*a*a+b*b*b*b*b*b+c*c*c*c*c*c+d*d*d*d*d*d+e*e*e*e*e*e+f*f*f*f*f*f)
	    	printf("%3d\n",i);
	    }
    }
    if(N==7)
    {
    	for(i=1000000;i<=9999999;i++)
    	{
	    	a=i/1000000;
	    	b=i%1000000/100000;
	    	c=i%100000/10000;
	    	d=i%10000/1000;
	    	e=i%1000/100;
	    	f=i%100/10;
	    	g=i%10;
	    	if(i==a*a*a*a*a*a*a+b*b*b*b*b*b*b+c*c*c*c*c*c*c+d*d*d*d*d*d*d+e*e*e*e*e*e*e+f*f*f*f*f*f*f+g*g*g*g*g*g*g)
	    	printf("%3d\n",i);
	    }
    }
    return 0;
}